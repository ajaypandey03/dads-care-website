# Base URL of dadscare-backend's REST API, used by the /dashboard/* client dashboard.
# Baked in at build time (NEXT_PUBLIC_* vars are inlined into the static export).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8090
